import { DetermineAgePipe } from './determine-age.pipe';

describe('DetermineAgePipe', () => {
  it('create an instance', () => {
    const pipe = new DetermineAgePipe();
    expect(pipe).toBeTruthy();
  });
});
